global:
  scrape_interval: 15s # Default scrape interval for all targets

scrape_configs:
  - job_name: otel-collector
    scrape_interval: 5s # Override scrape interval for this job
    static_configs:
      - targets: ["otel-collector:9464"] # Scrape OpenTelemetry Collector metrics
